"use client";
import {useState} from "react";
import {Bot, Send, Upload} from "lucide-react";
import {useLanguage} from "@/hooks/useLanguage";
export default function Assistant(){const {t}=useLanguage();const [messages,setMessages]=useState([{role:"ai",text:t.assistantWelcome}]);const [text,setText]=useState("");const send=()=>{if(!text.trim())return;const q=text.trim();setMessages(m=>[...m,{role:"user",text:q},{role:"ai",text:t.assistantDemoResponse}]);setText("")};return <div><div className="card flex min-h-[500px] flex-col"><div className="flex items-center gap-3 border-b p-4"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-fasai-100"><Bot className="text-fasai-700"/></div><div><b>{t.askAssistant}</b><div className="text-xs text-emerald-600">● {t.assistantDemo}</div></div></div><div className="flex-1 space-y-3 overflow-auto p-4">{messages.map((m,i)=><div key={i} className={`flex gap-2 ${m.role==="user"?"justify-end":""}`}><div className={`max-w-[80%] rounded-2xl px-4 py-3 ${m.role==="user"?"bg-fasai-600 text-white":"bg-slate-100 text-slate-700"}`}>{m.text}</div></div>)}</div><div className="border-t p-3"><div className="mb-2 flex gap-2"><label className="btn-secondary cursor-pointer px-3 py-2 text-sm"><Upload size={16}/>{t.uploadImage}<input type="file" accept="image/*" className="hidden"/></label></div><div className="flex gap-2"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==="Enter"&&send()} placeholder={t.typeMessage} className="min-w-0 flex-1 rounded-xl border p-3 outline-none"/><button onClick={send} className="btn-primary px-4"><Send size={18}/><span className="hidden sm:inline">{t.send}</span></button></div></div></div></div>}
